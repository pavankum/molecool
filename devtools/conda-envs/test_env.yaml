name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  
    # dependencies
  - numpy
  - matplotlib
  

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

